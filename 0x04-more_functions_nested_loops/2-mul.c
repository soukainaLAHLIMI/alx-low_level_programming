#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first numbers to multiply
 * @y: the second numbers to multiply with first number 
 * Return: the result of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return(x*y);
}
        
